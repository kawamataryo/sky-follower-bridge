// 初期開発
// 1. 表示されているユーザー情報（アカウント名・表示名・bio・プロフ画像URL）を取得する
// 2. 取得したユーザー情報をもとに、BSkyBlockのユーザー情報を検索する
// 3. 一致したものをダイアログに表示する
// 4. スクロールして1に戻る。もしスクロールできない場合は終了する
//
// キャッシュ
// twitterのユーザー情報とbskyのユーザー情報を一定期間キャッシュする（例えば3時間？）
// キャッシュがある場合は、キャッシュを使って表示する
// キャッシュの単位を一括にするか、ユーザーごとにするか・・
//
// モーダルの表示
// vanjsでのモーダル表示

const bulkSearchBskyUsers = async () => {
}
